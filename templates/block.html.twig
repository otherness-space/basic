<div id="{{ attributes.id }}" class="{{ attributes.class }} clearfix"{{ attributes }}>
  <div class="block-inner">

    {{ title_prefix }}
    {% if label %}
      <h2 class="block-title"{{ title_attributes }}>{{ label }}</h2>TESTING
    {% endif %}
    {{ title_suffix }}

    <div class="content"{{ content_attributes }}>
      {{ content }}
    </div>
  </div>
</div>